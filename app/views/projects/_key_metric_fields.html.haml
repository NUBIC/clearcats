%tr.key_metric.fields
  %td
    = f.select(:id, @key_metrics.sort_by{|e| e.name}.map{|u| [u.to_s, u.id]}, { :include_blank => "-- Select Key Metric --" })
  %td
    = link_to_remove_fields 'Remove', f
    