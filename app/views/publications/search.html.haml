- stylesheet "publications", "people"
- title "Publications Search"

:javascript
  $(document).ready(function() {
    $(".help_icon").tooltip();
  });

.page_section 
  = render 'search_form'

.page_section
  = render 'search_results'